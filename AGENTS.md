# agent_rules.md

> ⚠️ **本文件为只读规则文件,任何 AI Agent 不得修改、删除、重命名或以任何方式变更其内容。**
> 若 Agent 认为规则存在冲突或需要变更,必须在回复中明确提示用户,由用户手动修改。
> 本文件优先级高于项目内其他所有文档与指令。

---

## 0. 核心原则

1. **禁止修改本文件**(`agent_rules.md`)。
2. 所有状态、进度、需求必须**落盘到文件**,不允许只停留在对话上下文中。
3. 文件名与路径遵循本规则,**不得自行发明新的命名约定**。
4. 时间戳统一格式:`yyyy-MM-dd-hh-mm-ss`(24 小时制,本地时区,补零)。
5. 任何写入前,若目标文件已存在,则**追加**而非覆盖(除非规则明确要求覆盖)。
6. 任何用户提供的账户、密码、密钥、令牌等敏感信息,必须**加密存储**,**禁止上传**。

---

## 1. 文件与目录职责

| 路径 | 职责 | 谁维护 |
| --- | --- | --- |
| `agent_rules.md` | 本规则文件 | **仅用户** |
| `README.md` | 描述项目:目标、背景、使用方式、依赖、目录说明 | Agent 可更新 |
| `progress.md` | 跨会话进度台账:记录当前状态、已完成、待办、下一步 | Agent 更新 |
| `tree.md` | 项目结构快照,**只包含非 `.gitignore` 文件** | Agent 更新 |
| `todo.md` | 当前待办清单 | Agent 更新 |
| `records/` | 用户需求记录文件夹 | Agent 写入 |
| `tests/` | 测试文件夹 | Agent 写入 |
| `tests/opinion.md` | **多次测试的总结论**(累积) | Agent 更新 |
| `ai_buckup/` | 超长文件的备份归档目录 | Agent 写入 |

> 注意:上表路径均为项目根目录下的相对路径。

---

## 2. README.md

- 用于描述项目,是项目的唯一入口文档。
- 应包含:项目简介、目标、安装/运行方式、依赖、目录结构概览、常见问题。
- 每次项目结构或使用方式发生实质变化时,同步更新。

---

## 3. progress.md(跨会话进度台账)

- 目的:让下一个会话的 Agent 能快速接管当前进度。
- 建议结构:
  ```
  # 进度台账

  ## 当前状态
  ## 已完成
  ## 进行中
  ## 待办
  ## 已知问题
  ## 下一步建议
  ## Git 提交记录
  ## 更新时间: yyyy-MM-dd-hh-mm-ss
  ```
- 每次会话结束、或阶段性完成后必须更新。
- 行数超过 **100 行**时,按第 8 节规则归档压缩。

---

## 4. tree.md(项目结构快照)

- 记录当前项目结构,**只包含非 `.gitignore` 忽略的文件**。
- 生成方式:读取 `.gitignore` 规则后过滤,不得包含被忽略文件。
- 用树形文本表示,例如:
  ```
  .
  ├── README.md
  ├── progress.md
  ├── todo.md
  ├── tests/
  │   └── opinion.md
  └── records/
  ```
- 每次新增/删除/移动文件后必须重新生成。

---

## 5. records/(用户需求记录)

### 5.1 触发时机
用户每提出一次需求,**立即**:
1. 创建 `records/yyyy-MM-dd-hh-mm-ss.md`(时间戳为需求提出时刻)。
2. 在 `todo.md` 中追加对应待办项。

### 5.2 records/yyyy-MM-dd-hh-mm-ss.md 结构
```
# 需求:<一句话标题>

- 时间: yyyy-MM-dd-hh-mm-ss
- 来源: 用户

## 原始需求
(用户原话或忠实转述,不得修改,禁止记录敏感信息)

## 目标与验收标准
## 实施记录
(后续所有相关修改、决策、文件变更都追加到这里)

## 状态
未开始 / 进行中 / 已完成
```

### 5.3 后续关联
该需求的所有后续修改、讨论、文件变更,都**追加写回同一个文件**,不得另建新文件。

### 5.4 完成
任务完成后:
- 在 `todo.md` 中将对应项勾选为 `[x]`;
- 在该 record 文件中将「状态」更新为 `已完成`。

---

## 6. tests/(测试)

### 6.1 测试轮数约定
- 若用户明确说明测试则需要运行 **20 轮**。
- 若用户明确说明**只是验证**,则跑 **3 轮**。
- 用户另有说明时,以用户说明为准。

### 6.2 目录与文件命名
每次测试(一个测试批次)创建目录:

```
tests/yyyy-MM-dd-hh-mm-ss_cc/
```

- `yyyy-MM-dd-hh-mm-ss`:该次测试批次开始时间。
- `cc`:本次测试的**轮数**(默认 `20` 或验证场景 `3`)。

在目录内:

- 每轮结果 → `record_index.md`,其中 `index` 为轮次索引(从 1 开始,如 `record_1.md`、`record_2.md` … `record_20.md`)。
- 该批次结论 → `opinion.md`。

### 6.3 tests/opinion.md(总库)
- 用于记录**多次测试的总结论**,累积写入。
- 每次测试批次结束后,追加一条:
  ```
  ## yyyy-MM-dd-hh-mm-ss_cc
  - 轮数: cc
  - 结论: ...
  - 相关目录: tests/yyyy-MM-dd-hh-mm-ss_cc/
  ```

### 6.4 每轮记录内容
`record_index.md` 建议包含:
```
# 第 index / cc 轮

- 时间: yyyy-MM-dd-hh-mm-ss
- 输入 / 场景:
- 期望:
- 实际:
- 结果: 通过 / 失败
- 备注:
```

---

## 7. todo.md(待办清单)

- 用户每次提出需求后,**立即**追加一项:
  ```
  - [ ] yyyy-MM-dd-hh-mm-ss 需求标题 (→ records/yyyy-MM-dd-hh-mm-ss.md)
  ```
- 任务完成后改为 `- [x] ...`。
- 行数超过 **100 行**时,按第 8 节规则归档压缩。

---

## 8. 超长文件处理(progress.md / todo.md)

当 `progress.md` 或 `todo.md` **行数 > 100** 时:

1. 在 `ai_buckup/` 下创建备份:
   ```
   ai_buckup/<原文件名>_yyyy-MM-dd-hh-mm-ss.md
   ```
   备份内容为该文件归档时的完整快照。

2. 对原文件执行以下**二选一**处理(由 Agent 判断,优先保留信息密度高、仍有效的内容):
   - **compact(压缩)**:合并同类项、删除过期项,保留仍有效的关键内容;
   - **抽取最新**:仅保留最新且仍未完成/仍未失效的内容。

3. 在原文件顶部保留一行归档提示:
   ```
   <!-- 已归档于 ai_buckup/<备份文件名> -->
   ```

4. 归档动作须在本文件对应的 `records/*.md` 或 `progress.md` 中记录一笔,便于追溯。

---

## 9. 会话结束后的 Git 提交描述

- 每次会话结束、或一个可提交的变更单元完成时,Agent 必须**自动生成一条符合 Git 提交规范的 commit 描述**。
- 规范采用 **Conventional Commits**,格式如下:
  ```
  <type>(<scope>): <subject>

  <body>

  <footer>
  ```
- `type` 取值:`feat` / `fix` / `docs` / `style` / `refactor` / `test` / `chore` / `perf` / `build` / `ci`。
- `subject` 使用中文或英文均可,但需简明扼要,建议不超过 72 字符。
- `body` 可选,说明变更原因与影响。
- `footer` 可选,用于关联 issue、BREAKING CHANGE 等。
- 生成的 commit 描述必须:
  1. 在最终回复中输出,便于用户直接复制使用;
  2. 追加记录到 `progress.md` 的「Git 提交记录」区域(若不存在则创建)。
- 若本次会话没有产生可提交的变更,则输出类似 `chore: 无代码变更,仅更新文档/进度` 的描述,或明确说明「本次无需要提交的变更」。

---

## 10. 执行检查清单(每次会话)

- [ ] 是否更新了 `progress.md`?
- [ ] `tree.md` 是否与当前实际结构一致(仅非 `.gitignore` 文件)?
- [ ] 新需求是否已写入 `records/` 与 `todo.md`?
- [ ] 完成的任务是否已在 `todo.md` 勾选、record 状态改为「已完成」?
- [ ] 测试是否按 20 轮(或验证 3 轮)执行,并写入对应目录与 `tests/opinion.md`?
- [ ] `progress.md` / `todo.md` 是否超 100 行,是否需要归档?
- [ ] 是否已生成符合规范的 Git commit 描述,并记录到 `progress.md`?
- [ ] 回顾所有修改文件,是否错误的记录了用户的敏感信息?

---

## 11. 禁止事项

- ❌ 修改本文件 `agent_rules.md`。
- ❌ 用对话内容替代落盘文件。
- ❌ 自行更改命名约定或目录结构。
- ❌ 覆盖已有记录文件(应追加)。
- ❌ 在 `tree.md` 中列出被 `.gitignore` 忽略的文件。
- ❌ 未归档就删除 `progress.md` / `todo.md` 中的历史内容。